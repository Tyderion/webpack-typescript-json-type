const assert = require('assert');
const webpackTypescriptJsonType = require('../index.js');

describe('webpackTypescriptJsonType', () => {
  it('has a test', () => {
    assert(false, 'webpackTypescriptJsonType should have a test');
  });
});
